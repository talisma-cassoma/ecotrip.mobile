name: EAS Optimized Deploy

on:
  push:
    branches:
      - main
      - staging

jobs:
  ota_or_build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20 # Atualizado para a versão 20 (Resolve o erro do minimatch)
          cache: 'npm'

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EAS_UPDATE_ACCESS_TOKEN }} # Sua secret atual

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Set profile and branch variables
        run: |
          if [ "${{ github.ref_name }}" == "staging" ]; then
            echo "PROFILE=staging" >> $GITHUB_ENV
            echo "BRANCH=staging" >> $GITHUB_ENV
          else
            echo "PROFILE=production" >> $GITHUB_ENV
            echo "BRANCH=production" >> $GITHUB_ENV
          fi

      # Publica a atualização OTA (JS/Assets)
      - name: Publish OTA update
        run: eas update --branch ${{ env.BRANCH }} --platform all --non-interactive --message "${{ github.event.head_commit.message }}"

      # Opcional: Gera o build (APK) apenas se o commit tiver "[build]" no texto
      - name: Build Android APK
        if: contains(github.event.head_commit.message, '[build]')
        run: eas build --platform android --profile ${{ env.PROFILE }} --non-interactive --no-wait